---
id: D-06
title: "Device list & persistence (Room)"
epic: Device Management
epic_label: epic/devices
milestone: v1
priority: priority/high
estimate: estimate/M
status: status/todo
---

# D-06 Device list & persistence (Room)

Persist paired devices and expose them as a Flow.

## Acceptance criteria

- [ ] `DeviceEntity` matches schema in [Architecture § Data model](../architecture.md#data-model-room)
- [ ] `DeviceDao` with flow queries: `observeAll()`, `getById(id)`
- [ ] `DeviceRepository` exposes `Flow<List<Device>>` mapped to domain model
- [ ] Add device → name editable → save
- [ ] Delete device → also remove token (D-05) and any scene targets referencing it (cascade)
- [ ] Reorder devices (drag handle)
